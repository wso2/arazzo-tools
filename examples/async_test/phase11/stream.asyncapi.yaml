asyncapi: 3.0.0
info:
  title: Event Stream (kafka — no adapter yet)
  version: 1.0.0
servers:
  cluster:
    host: kafka.example.com:9092
    protocol: kafka
channels:
  events:
    address: click-events
    messages:
      event:
        contentType: application/json
        payload:
          type: object
