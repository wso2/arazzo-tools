# Phase 9 — Scenario 4: RECEIVE TIMEOUT (fails on purpose). Nothing is sent, so the receive waits
# up to its `timeout` and then fails — the step (and workflow) end in error instead of hanging forever.
#
# EXPECTED: run **timeoutFlow** → `await` waits ~3000ms then fails:
#   receive on channel "orders/new" timed out after 3s: no message arrived
# (a receive WITH a correlationId that times out instead says: no message matching correlationId "…" arrived)

arazzo: 1.1.0

info:
  title: "Phase 9 - receive timeout"
  version: "1.0.0"

sourceDescriptions:
  - name: orderBus
    url: ./order-events.asyncapi.yaml
    type: asyncapi

workflows:
  - workflowId: timeoutFlow
    steps:
      - stepId: await
        channelPath: orderBus#/channels/orders
        action: receive
        timeout: 3000                       # nothing is ever sent -> times out -> fails
        successCriteria:
          - condition: '$message.payload.status == "new"'
